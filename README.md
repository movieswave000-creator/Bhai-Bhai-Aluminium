# Bhai Bhai Aluminium Industries — V9

V9 fixes admin save reliability on Windows/localhost and Render by using a cross-platform JSON write and `secure: auto` sessions. Dark mode is the default; light mode remains available. Visual layers are kept behind content and the three glass lines are limited to the left half.
